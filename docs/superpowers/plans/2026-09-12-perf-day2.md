# Perf Day 2 — plan (written 2026-09-11 night, executed 2026-09-12)

> **Status 2026-09-12 midday:** Tasks 1, 2, 3, 4, 7, 8 DONE (commits a3ff286c … 3408f216). Physics (6) skipped by Sam's call. Grass GPU path (4 in the old numbering, now 'Task 4: Grass on the GPU') still dormant. Dev-build frame went 9.0 → 7.5 ms before the UI split; the UI split + galaxy clock vault + idle-pulse gates came after and are unmeasured. Next: Sam's release-build baseline.

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** more fps with ZERO gameplay or look change. Start of day 1: 50-60 fps. End of day 1: 120 in the village, 140-150 in empty spots (laptop RTX 4060, 1080p); desktop 3700X: CPU 8-9 ms vs GPU 3-4 ms — the game is single-core CPU bound.

**Architecture:** every task removes main-thread work: fewer separate objects to submit (combine + instance), fewer canvas rebuilds, fewer per-frame recomputations. Each task ships behind a `FeatureVault` flag or is a pure asset/setting change, and each gets ONE build check by Sam (release build, no dev tools).

**Tech stack:** Unity 2022.3 built-in RP, `MeshCombineTool` (Editor, `Assets/3 - Scripts/Editor/MeshCombineTool.cs`), `PerfTrace` (dev builds only; `docs/PLAYTEST_PERF_TRACE.md`).

**Ground rules:** compile-check with `py -3 prototypes/shuttle-computer/test/compile-unity.py` before every commit; Sam runs every playtest; tag a milestone after each verified win; never touch the Celestial/atmosphere zone.

**Where the frame goes now (main thread, ~8-9 ms, from the run-6 profiler minus fixes):**
camera cull/sort/submit 2.8 · grass 2.4 (per-blade CPU rebuild each frame) · canvases 1.5 · dust 0.9 · Update scripts 0.9 · physics 0.9 · engine/animators/GI 1.5.

---

### Task 1: Combine the shuttle's static parts (biggest CPU win, ~1 ms+)

`Shuttle_Lander` = 248 MeshRenderers, 236 shadow casters; every part is submitted 4× per frame (colour, depth, 2 cascades). Hand-maintained prefab (`Assets/5 - External Imports/Prefabs/Shuttle_Lander.prefab` — verify with Glob).

- [ ] Inventory (read-only, Python over the prefab YAML like `scratchpad/scene_census.py` did): list every renderer, its mesh source `.fbx` and the `.meta` `m_IsReadable` flag, and every object with a MonoBehaviour / Animator / Rigidbody / HingeJoint on it or an ancestor (doors, screens, `ShuttleComputerWorldScreen`, `ShuttleThrustFX` lights, buttons, fans). Those stay separate.
- [ ] For every source model that is not readable: set `m_IsReadable: 1` in its `.fbx.meta` (Unity reimports; low-poly, small memory cost). Commit.
- [ ] Add a runtime combiner `Assets/3 - Scripts/World/StaticClusterCombiner.cs` (auto-singleton, `FeatureVault.ClusterCombine`): for a named root, take MeshRenderers whose object AND ancestors below the root carry only Transform/MeshFilter/MeshRenderer/Collider, group by (sharedMaterials, layer, shadowCastingMode, receiveShadows), `Mesh.CombineMeshes` with `mergeSubMeshes=false`, `useMatrices=true` relative to the root, one child object per group named `<root> Combined N`, disable the originals' renderers (keep colliders). Skip skinned, disabled, MaterialPropertyBlock users and anything named in an exclusion list (`Screen`, `Door`, `Fan`, `Button`, `Light`, `Glass`).
- [ ] Apply to roots: `Shuttle_Lander`, then `MoonBaseINTER`, `Fish_market_with_sections`, `StartCabin`. Log `[StaticClusterCombiner] <root>: N renderers → M`.
- [ ] Sam's check: shuttle looks identical inside/outside, doors + screens + cabin lights still work, moon base doors/buttons work, fish market intact. Expect draws in the shuttle view 3000 → ~1200.

### Task 2: Cast Shadows Off on tiny parts (doc B5)

236 casters under 35 cm (tunnel cage lights ×132, shuttle bits ×59, village bits ×20) cost a shadow-map draw per cascade each and draw no visible shadow.

- [ ] Editor script `Assets/3 - Scripts/Editor/TinyShadowCasterFix.cs` (menu `Tools ▸ Optimize ▸ Cast Shadows Off On Tiny Renderers`): for every enabled MeshRenderer in the open scene + the prefabs `Shuttle_Lander`, `TunnelCageLight`, `MoonBaseINTER` whose `bounds.size` max axis < 0.35 m, set `shadowCastingMode = Off`; log the count; Sam saves the scene / prefabs.
- [ ] Check: no visible shadow change on normal props (a bolt's shadow is sub-pixel).

### Task 3: UI canvases (~1.5 ms)

One changing element re-batches its whole canvas. Movers: the galaxy clock text, the compass strip, the vitals bars, the fishing tension bar, the FPS overlay (own canvas already).

- [ ] Grep `AddComponent<Canvas>` and the scene's `!u!223` documents to map which canvas each HUD script builds on (`VitalsHUD`, `CompassHUD`, `GalaxyTimeHUD`, `FishingTensionHUD`, `CatPerkHUD`, `FireflyGlowHUD`, `Hotbar`, `InteractPromptUI`).
- [ ] For each per-frame mover that shares a big canvas: add a nested `Canvas` (+ `GraphicRaycaster` only if it had one) on the mover's root object at runtime in that script's build step, `overrideSorting = false`, so only its own geometry rebuilds. Do it one script at a time, compile, commit.
- [ ] Also: TMP text that is rewritten every frame — gate with change detection where missing (`if (value != _last)`), and for the galaxy clock only update once per second.
- [ ] Check: HUD draws in the same order (nothing hidden behind something else), pause menu and dialogue unaffected.

### Task 4: Grass on the GPU, done properly (~2 ms)

Day-1 attempt (`FeatureVault.GrassGpuBatches`, dormant in `InstancedGrassRenderer.cs`; shaders reverted) produced see-through "glassy" grass when turning = the colour pass and the depth pre-pass disagreeing. Needs a DEV build + Frame Debugger.

- [ ] Re-apply the shader edit from `git show 7c2333e6` to a COPY of the two shaders first; build a dev build; in the Frame Debugger confirm (a) the `AfterDepthTexture` command buffer draws the same batches as the colour pass, (b) `_GrassInstanceOffset` is non-zero in BOTH passes' constant buffers, (c) `unity_WorldToObject` in the surface shader's vertex function is the per-instance inverse (if not: compute the object-space offset on the CPU per batch instead — all blades in a batch share the planet rotation, so pass `_GrassInstanceOffsetObj = R⁻¹·t` as the MPB value and use it directly without `unity_WorldToObject`).
- [ ] Fallback design if (c) fails: keep world-space matrices but rebuild only when the visible set changes, and move the translation into a per-draw MPB float3 added in WORLD space after the object-to-world multiply (surface shader: modify `v.vertex` via `mul(unity_WorldToObject, float4(t,0))` is the same thing — so the real fix is to stop using `unity_WorldToObject` and precompute per batch).
- [ ] Only flip the flag back on after the Frame Debugger shows both passes identical; Sam checks grass against the sky at the horizon in a BUILD (the documented build-only stripping trap).

### Task 5: Dust to quarter rate (~0.4 ms, look: twinkle refresh)

- [ ] In `SpaceDustField.LateUpdate` change `int parity = Time.frameCount & 1` / `(i & 1)` / `dt * 2f` to `& 3` / `dt * 4f`. Compile, commit. Check: dust still streams into the black hole, no stepping.

### Task 6: Physics 100 Hz → 50 Hz (~0.5 ms, FEEL check)

- [ ] `Universe.cs` fixedDeltaTime 0.01 → 0.02 (see `PhysicsRate`). Rigidbody interpolation must be ON for the player and the shuttle (check `m_Interpolate` in `NetworkPlayer.prefab` / `Player.prefab` / `Shuttle_Lander`).
- [ ] Sam feel-tests jump, landing, fishing tow, shuttle landing, swimming. Revert if anything feels off; the orbits are clockwork rails now so determinism is not a concern (doc C4).

### Task 7: Realtime GI off (~0.25 ms + 8 threads, LOOK check)

- [ ] Scene `LightmapSettings`: `m_EnableRealtimeLightmaps: 1` with `m_LightingDataAsset: {fileID: 0}` (no data). Set it to 0 in the Lighting window (Sam saves the scene). Check ambient light on the day and night side is unchanged; if the ambient shifts, revert — `m_RealtimeEnvironmentLighting` may be feeding the ambient probe.

### Task 8: Camera-side leftovers

- [ ] Confirm in a dev build that only the main camera renders per frame on foot (`FishPreviewCamera`, `MapCamera`, `ShuttleScreenCam`, `BuildMenu_PreviewCamera`, `FishingdexPreviewCamera` all disabled until used; `WishCage` `MirrorReflection` only when on screen).
- [ ] `AmbientFishField.LateUpdate` allocates 575 KB every ~15 s (the visible hitch): find the re-population and reuse its lists/arrays.

### Task 9: Milestone

- [ ] After each verified task: `git tag perf-milestone-2026-09-12-<n>`; push branch + main + tag to `soundofspace`.

---

**Order:** 1 → 2 → 3 → 5 → 8 (all look-neutral) → 6 (feel) → 7 (look) → 4 (needs the Frame Debugger session). Expected: main thread 8-9 ms → ~5-6 ms on the desktop = 144+.

**Measurement:** dev build + `PerfTrace` CSV (`py -3 tools/perf/analyze_perf_trace.py`) and the live Profiler dump (`Tools ▸ Solar System ▸ Perf ▸ Dump CURRENT Profiler Frames To Text`). Frame Count is 2000 max.
